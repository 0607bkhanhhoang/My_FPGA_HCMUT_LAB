transcript on
if {[file exists rtl_work]} {
	vdel -lib rtl_work -all
}
vlib rtl_work
vmap work rtl_work

vlog -vlog01compat -work work +incdir+D:/Study/DSP\ FGPA/LAB1/rectangle/sine_waves_generator {D:/Study/DSP FGPA/LAB1/rectangle/sine_waves_generator/sine_wave_generator.v}

